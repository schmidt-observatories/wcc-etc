# All dependencies are declared in pyproject.toml.
# Install the package with:
#   pip install -e .           # core
#   pip install -e ".[dev]"    # + dev tools (pytest, ruff, mypy, pre-commit)
#   pip install -e ".[docs]"   # + documentation build (sphinx etc.)
