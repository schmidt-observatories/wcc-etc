# Documentation dependencies are declared in pyproject.toml under [project.optional-dependencies].
# Install with:
#   pip install -e ".[docs]"
#
# Note: nbsphinx also requires a pandoc binary on the build machine.
#   brew install pandoc  /  conda install pandoc  /  apt-get install pandoc
